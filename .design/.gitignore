# Generated by the design-canvas seeder from the .dc.html sources + canvas.json.
# ~2.5 MB and fully reproducible, so it is not tracked.
propeloseo-homepage-directions.html

# Scratch: local render/measure tooling and the flattened copy used for scoring.
node_modules/
measure.mjs
_eval.html
